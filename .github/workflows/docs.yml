name: ReadTheDocs Preview

on:
  workflow_dispatch:

permissions:
  pull-requests: write

jobs:
  preview-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: readthedocs/actions/preview@v1
        with:
          project-slug: "hetu-galvatron"